const siteUrl = "https://www.cryptoeducating.net/";
module.exports={
    siteUrl,
    generateRobotsTxt: true,
    additionalSitemaps:[
        `${siteUrl}sitemap.xml`,
        `${siteUrl}server-sitemap.xml`,
    ],
};